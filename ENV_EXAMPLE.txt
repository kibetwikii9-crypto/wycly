# Business Management System Environment Variables
# Copy this file to .env and fill in your actual values

# Application URLs
PUBLIC_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000

# Database Configuration (Supabase PostgreSQL)
DATABASE_URL=postgresql://user:password@host:port/database

# Security Configuration
# IMPORTANT: Generate a strong random secret key for production!
# You can generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-change-in-production

# Logging
LOG_LEVEL=INFO

# OpenAI API Key (optional - leave empty for rule-based AI)
OPENAI_API_KEY=

# Note: BOT_TOKEN and ADMIN credentials are not needed here.
# Users connect their own Telegram bot tokens through the dashboard.
# Users create their own accounts through the registration endpoint.

